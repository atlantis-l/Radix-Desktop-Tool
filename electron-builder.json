{
  "appId": "indi.atlantis-l.app.radix-desktop-tool",
  "asar": true,
  "productName": "Radix Tool",
  "directories": {
    "output": "release"
  },
  "files": ["dist", "dist-electron"],
  "mac": {
    "target": ["dmg"],
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}"
  },
  "win": {
    "target": ["portable"],
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}"
  },
  "linux": {
    "target": ["AppImage"],
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}"
  }
}
